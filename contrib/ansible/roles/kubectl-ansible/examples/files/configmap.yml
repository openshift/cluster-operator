kind: "ConfigMap"
apiVersion: v1
metadata:
  name: sampleconfig
data:
  configfile.cfg: |
    logLevel: 4
    clusters:
    - name: clustername
    protected:
    - default
    - kube-public
    - kube-system
